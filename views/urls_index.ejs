<!DOCTYPE html>
<html lang = "en">
  <head>
  <%- include ./partials/_header %>
    <title>
      TinyApp
    </title>
  </head>
  <body>
    <%- include ./partials/_navbar %>

    <h2 id="startingText">All URL</h2>
    <h2 style="text-align:center;">Want to add a URL ? <a href="/urls/new" class="url">URL shortener</a></h2>
    <table>
    <% for (let shortones in urls) { %>
        <tr>
          <th>
            <a href=<%= urls[shortones] %> target="_blank" class="url">http://localhost:8080/<%= shortones %></a>
          </th>
          <th>
            (<%= urls[shortones]%>)
          </th>
          <form method="GET" action=/urls/<%= shortones %>>
          <th>
            <button type="submit" class="btn delete" >edit</button>
          </th>
          </form>
          <form method="POST" action=/<%= shortones %>/delete>
            <th>
              <button type="submit" class="btn delete" >delete</button>
            </th>
          </form>
        </tr>
    <% } %>
    </table>
  </body>
</html>